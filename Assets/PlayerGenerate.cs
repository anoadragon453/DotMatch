// Class to spawn random dots for the user to change and send out.

using UnityEngine;
using System.Collections;

public class PlayerGenerate : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
